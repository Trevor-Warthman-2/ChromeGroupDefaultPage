import { AsyncInitialization } from '../models/AsyncInitialization.ts';
import { GroupDefault } from '../models/GroupDefault.ts';

export class Store extends AsyncInitialization {
  groupDefaults: Array<GroupDefault>;

  constructor() {
    
  }

  #initialize(): void {
    const chromeStore = sto
    this.groupDefaults = 
  }
  
}

export default Store;
